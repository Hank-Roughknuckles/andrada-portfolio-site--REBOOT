<% provide( :title, "Edit Landing Page" ) %>

<%= form_for @landing_page_photo, 
  url: landing_page_path(@landing_page_photo) do |f| %>
  <%= f.label :link %>
  <%= f.text_field :link %>
  <%= f.submit "Save" %>
<% end %>

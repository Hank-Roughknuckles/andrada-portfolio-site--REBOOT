<% if params[:action] =~ /show/ %>
  
  <% if video_collection.is_root? %>
    <% destination = video_collections_path %>
  <% else %>
    <% destination = video_collection.parent %>  
  <% end %>

  <%= link_to "Back", destination %>
<% end %>
